{
  "scripts": {
    "pre-build-app": "cd ./frontend && npm install cd .././backend && npm install",
    "build-app": "cd ./frontend && npm run build",
    "start-app": "cd ./backend && npm run start"
  }
}
